stages:

  - stage: BuildServices

    pool:
      vmImages: ubuntu-lates

    jobs:
    - job: BuildDocker_SalesBookLB
      displayName: Build Docker Image for SalesBook.LB app
      steps:

      - task: Docker@2
        inputs:
          command: 'buildAndPush'
          Dockerfile: '**/Dockerfile'
          tags: |
            $(Build.BuildId)
            lates